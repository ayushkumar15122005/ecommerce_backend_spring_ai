spring:
  application:
    name: ecommerce-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/ayush
    username: postgres
    password: Ayush@1512

  jpa:
    hibernate:
      ddl-auto: validate   # Flyway owns the schema, Hibernate only validates it
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

  ai:
    openai:
      api-key: sk-proj-8LqvP2mX9rNcA7ZdYwEf4HtKuJmQxRsBnCvLpTgH5sF2aWkY
      chat:
        options:
          model: gpt-4o-mini

server:
  port: 8080

app:
  jwt:
    secret: ah19jln9JDwTrFY9HnGQ3SINKaN8QqchrCiLqkZbdFt
    access-token-expiration-ms: 900000       # 15 minutes
    refresh-token-expiration-ms: 604800000   # 7 days

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

logging:
  level:
    com.ecommerce: DEBUG
    org.springframework.security: INFO
